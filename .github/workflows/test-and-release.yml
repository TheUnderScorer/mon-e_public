name: Dispatch test & release

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - test

jobs:
  dispatch:
    name: Test run
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
